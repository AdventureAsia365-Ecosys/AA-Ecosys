# Session Log — S183 (AA-Ecosys dev)

- **Ngày:** 2026-09-16
- **Agent:** Kiro
- **Chủ đề:** CIS Data Reset G0-G3 (backup, điều tra, script, reset thật) + phát hiện luồng N7/N8 dead + audit UI số liệu residual.
- **Trạng thái:** G0-G3 Done (data reset commit thành công). G4+ chưa chạy — Nghiệp yêu cầu nghiên cứu Bedrock Batch trước + audit UIUX + kiểm tra dedup upload.

---

## 1. Trạng thái

- Đầu phiên: đọc memory S182 + log + Linear → tiếp nối chuỗi audit CIS. G0 (AA-595) là bước đầu.
- Cuối phiên: **G0-G3 Done** trên Linear (AA-595/596/597/598). Issue mới AA-603 (cleanup DROP N7) tạo xong.
- **DB production đã RESET**: ~21.6k row dẫn xuất xóa, raw_tours=793 giữ nguyên (tất cả pipeline_status='ingested'). RDS snapshot `aa-cis-dev-db-g0-audit-20260916` là net an toàn.

## 2. Thay đổi CODEBASE

### AA-Ecosys (repo gốc, chưa commit)
- `docs/audit-cis-g1-before-snapshot.md` — snapshot "before" đầy đủ (70 bảng, row counts, pipeline_status, tenant analysis, ranh giới GIỮ/XÓA, bảng ngoài bản đồ).
- `docs/audit-cis-g3-reset.sql` — plan + dry-run script (comment, dùng để review).
- `docs/audit-cis-g3-execute.sql` — transaction thật đã chạy (BEGIN → DELETE 31 bảng + UPDATE raw_tours + DROP unknown_ledger → COMMIT).
- `.g0_*`, `.g1_*`, `.g2_*`, `.g3_*` — file tạm (query output, pgpass, tunnel log). **Dọn .g0_pgpass (chứa password)** khi xong hoàn toàn hoặc đầu phiên sau.
- `docs/backups/2026-09-16/` — 6 dump pg (shared 803KB, silver 2.2MB, gold 233KB, acp_contract 614KB, acp_shared 390KB, tripplanner 328KB). Gitignored.

## 3. Thay đổi HẠ TẦNG

### AWS acc2 (005097885195, us-west-1)
- **RDS snapshot** `aa-cis-dev-db-g0-audit-20260916` — manual, available. Giữ cho tới khi audit kết thúc thành công.
- **EC2 bastion** `i-006e9b5bc05c6861f` (t3.micro, aa-cis-dev-private-1, SSM) — **ĐANG CHẠY**, tính phí ~$0.01/h. Terminate ở AA-602.
- **IAM** `aa-cis-g0-bastion-role` + `aa-cis-g0-bastion-profile` — tạo mới. Dọn cùng bastion.
- **DB production aa_cis_dev** — đã reset dẫn xuất (G3 commit). raw_tours=793 ingested. Bảng giữ: destinations=379, search_demand=480, tenants=4, llm_call_log=741, tenant_api_usage=18050, pipeline_runs=38.
- **KHÔNG sửa rule mạng nào** (bastion dùng lại SG aa-cis-dev-sg-app, RDS SG không đổi).

## 4. Bằng chứng verify

- G0: snapshot available, 6 dump pg_restore --list exit 0, tunnel psql SELECT → aa_cis_dev|aa_cis_admin|15.17.
- G1: 70 bảng đếm row, pipeline_status (717+75+1=793), tenant (chỉ WanderLux có data dẫn xuất).
- G2: FK graph 80 FK query pg_constraint. Dry-run 33 bảng count khớp G1.
- G3: transaction COMMIT exit 0. POST-DELETE verify: mọi bảng dẫn xuất = 0, raw_tours=793 all ingested, GIỮ destinations/search_demand/tenants khác 0.
- 2 lần rollback trước commit (FK thứ tự sai: review_queue↔tenant_tour_versions, quarter_plan circular FK) — đã sửa, transaction cuối commit sạch.

## 5. Linear

| Issue | Trạng thái | Ghi chú |
|-------|-----------|---------|
| AA-595 (G0) | **Done** | Snapshot + bastion + tunnel + 6 dump |
| AA-596 (G1) | **Done** | Điều tra read-only, snapshot "before" |
| AA-597 (G2) | **Done** | FK graph + phân loại bảng + script + dry-run, Nghiệp duyệt |
| AA-598 (G3) | **Done** | Reset transaction commit thành công |
| **AA-603** (mới) | Backlog (Low) | [CLEANUP] DROP cụm N7/N8 dead sau gỡ code reader + deploy |
| AA-599 (G4) | Backlog | Chưa chạy — chờ batch S1 + audit UIUX |
| AA-600-602 | Backlog | G5/G6/cleanup chờ G4 |

## 6. CÒN LẠI — việc đầu phiên sau (S184)

### Ưu tiên 1: Reset triệt để — xóa số liệu residual
Nghiệp chốt góc độ: **reset TOÀN hệ thống** = chỉ giữ 793 raw tours + quá trình upload, mọi số liệu khác = 0 từ đầu. Các bảng GIỮ ở G3 nhưng chứa lịch sử cũ cần XÓA thêm:
- `shared.llm_call_log` (741) — lịch sử LLM call cũ → xóa (UI LLM Usage đọc bảng này)
- `shared.tenant_api_usage` (18050) — lịch sử API call tenant → xóa (UI Tenants tab đọc)
- `shared.pipeline_runs` (38) + `shared.pipeline_jobs` (46) — lịch sử run cũ → xóa (UI Master Content + Tenants Pipeline tab)
- `shared.notifications` (39) — thông báo cũ → xóa
- `shared.audit_log` (127) — log audit cũ → xóa
- `shared.prompt_eval_runs` (3) — run đánh giá → xóa
- `shared.llm_role_config` (16) — config LLM model → **GIỮ** (config, không phải số liệu)
- `shared.admin_users` (4) — **GIỮ** (admin account)
- `shared.schema_versions` (129) — **GIỮ** (migration tracker)
- `shared.membership_plans` (5) — **GIỮ** (plan config)
- Kiểm tra `acp_contract.atom_decompose_jobs` (87, dead) — xóa luôn?
- HỎI Nghiệp duyệt danh sách xóa thêm này trước khi chạy.

### Ưu tiên 2: Nghiên cứu Bedrock Batch Inference
Xây endpoint batch ~100 tour/lần S1 rewrite thay vì từng tour. Kiểm tra Bedrock có hỗ trợ batch inference API không. Nghiệp nhớ trong code có thiết kế batch (max 100) sẵn. Điều tra codebase + Bedrock docs.

### Ưu tiên 3: Kiểm tra dedup/update khi upload tour
Kiểm tra 793 raw_tours: có cơ chế chống duplicate khi upload cùng file/tour trùng tên không? Có cách update nội dung tour đã upload (trùng tên nhưng nội dung thay đổi) chưa? Đọc `services/ingestion/` + `upload_staging` + Excel parser.

### Ưu tiên 4: Audit UIUX toàn diện
- Số liệu mọi trang admin đã phản ánh đúng trạng thái "reset từ đầu" chưa?
- Phong cách UI admin đã đồng bộ với trang chính adventure.asia chưa?
- N7 model config (Settings > LLM Models) vẫn hiện → ẩn/xóa (AA-603).
- "788 source tours" ≠ 793 → điều tra query.

### Hạ tầng
- **Bastion `i-006e9b5bc05c6861f` ĐANG CHẠY** (~$0.01/h). Terminate khi xong hoàn toàn audit (AA-602). Nếu phiên sau cần tunnel, giữ; nếu nghỉ lâu, terminate rồi dựng lại.
- SSM tunnel cần restart mỗi phiên (idle timeout ~20 phút). Port **15432** (không dùng 5432 — WSL có local pg16 chiếm).
- Dọn `.g0_pgpass` (chứa password DB) khi xong.

## 7. Lưu ý kỹ thuật

- **Terminal WSL**: interactive zsh mangle multi-line/heredoc (double-char echo, stuck ∙ continuation). WORKAROUND: `/usr/bin/env bash -c "..."` 1 DÒNG, escape `$` thành `\$`, ghi output ra file `.g0_*/.g1_*/.g2_*/.g3_*` rồi read_file. KHÔNG dùng fs_write cho file WSL fs (bất nhất — tạo trên 1 layer khác). KHÔNG dùng heredoc/multi-line.
- **fs_write** ghi file ĐƯỢC đọc qua `\\wsl.localhost\...` (read_file, str_replace OK) NHƯNG shell commands (`bash -c`) CŨNG đọc được (khác issue G0 ban đầu — chỉ `.g0_trust.json` lúc đầu bị; các file docs/ ghi sau hoạt động tốt). Chưa rõ pattern nào fail/pass — dùng shell redirect cho file nhỏ/cần shell đọc, dùng fs_write cho docs dài.
- **SSM tunnel idle timeout**: ~20 phút không traffic → disconnect. Restart bằng `control_bash_process` (background). KHÔNG dùng port 5432 (local pg16 chiếm).
- **sudo trong WSL**: cần password interactive → kẹt execute_bash shell. Tránh sudo. Local pg restore dùng peer/trust nếu cần (chưa thử thành công).

## 8. Phát hiện quan trọng phiên này

1. **Luồng N7/N8 Produce đã CHẾT** — bản đồ audit S182 thiếu cả cụm `acp_deliver.*` + `acp_shared.acp_v2_*` + `unknown_ledger`. `acp_deliver.pieces` (135 rows) KHÔNG phải ACPv1 dead (sai AA-477) mà là output ACPv2 N7 dead (bị T-series thay). Đã xóa data, tách DROP bảng sang AA-603.
2. **WSL local postgres 16 chiếm port 5432** — root cause psql "password auth failed" ban đầu (kết nối nhầm local pg thay vì tunnel RDS). Tunnel phải dùng localPortNumber=15432.
3. **Circular FK** quarter_plan ↔ quarter_plan_version — cần `UPDATE current_version_id=NULL` trước khi DELETE version.
4. **review_queue → tenant_tour_versions** FK — phải xóa review_queue TRƯỚC tenant_tour_versions (ngược thứ tự bản đồ S182).
