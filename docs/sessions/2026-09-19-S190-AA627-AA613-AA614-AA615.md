# S190 — 2026-09-19 (Kiro, AA-CIS-App)

Redesign social tenant T7-T11 (tiếp S186/S189): làm trọn **AA-627** (DFS balance alert),
**AA-613** (gate severity 3-mức), **AA-614** (FE tenant My Content phẳng + copy-lock),
**AA-615** (FE admin gate telemetry). Đóng **AA-538** (không còn hợp lệ).

## Trạng thái (cuối phiên)
| Issue | Kết quả | PR |
|-------|---------|----|
| AA-627 | **Done** — DFS balance daily alert (EventBridge Scheduler → Lambda → POST) | App #408, Infra #63 + fix #65 |
| AA-538 | **Canceled** — tiền đề không còn đúng sau AA-515 (MAX_SUGGESTIONS=1, không có vòng lặp để batch) | — |
| AA-613 | **Done** — 3-tier gate severity + flat tenant + export/publish audit | #409 (PR-A) + #410 (PR-B) |
| AA-614 | **Done** — FE tenant My Content phẳng + copy-lock + export modes | #411 |
| AA-615 | **Done** — FE admin gate telemetry per tenant/channel | #412 |

Repo `apps/AA-CIS-App` sạch, ở `main`. 3 nguồn (Notion memory, log này, Linear) nhất quán.

## Thay đổi Codebase (AA-CIS-App)
**AA-627** (App #408, commit ac75a24):
- migration 159 `dfs_balance_snapshot`; `fetch_balance` (đọc balance FREE của DFS, cost=0);
  endpoint `admin_llm_ops` (`GET /admin/dfs-balance`, `POST /admin/dfs-balance/check`); FE banner cảnh báo số dư thấp.

**AA-613** chia 2 PR (giảm rủi ro):
- PR-A #409 (0b45e1e, low-risk): fix bug blog publish (markdown→HTML fragment, trước đẩy raw markdown vào content_html →
  WordPress hiện literal `##`/`**`); `export.py` tách 2 mode `document`/`fragment`; audit `CONTENT_EXPORTED`/`CONTENT_PUBLISHED`.
- PR-B #410 (2c731d3, behavior-changing): F9 3-mức severity (dùng lại flag `blocking` sẵn có, KHÔNG thêm cột/migration);
  tenant luôn nhận bản tốt nhất; `held` = "product-truth chưa giải quyết sau ≤2 retry"; tenant-safe projection
  (`_tenant_safe_piece` bỏ `status`/`held_reason`/`gate_ledger`); publish gate 422 cho held.

**AA-614** (#411, a772512):
- Backend: `_READY_STATE_MAP` held→**ready** (hội tụ `fetch_review`/`fetch_review_list` về cùng quy tắc `_tenant_safe_piece`
  đã dùng). `not_ready` giờ chỉ nghĩa "không có nội dung" (failed/processing). Fix contract break: `export_piece` đọc
  `ready_state` thay vì `piece["status"]` (đã vỡ từ AA-613 PR-B).
- FE `AngleGateWizard.tsx`: migrate `interface ContentPiece` → shape tenant-safe (`ready_state`) ở 5 chỗ đọc `.status`.
- FE `ReviewList.tsx`: copy-lock vùng hiển thị (`userSelect:none` + chặn `onCopy`/`onCut`/`onContextMenu` + toast "Use Export"),
  CHỪA textarea edit. Blog export 2 nút: "Export HTML file" (document) + "Export HTML for CMS" (fragment).

**AA-615** (#412, b1b893b):
- Backend `admin_a4.py`: endpoint mới `GET /admin/a4/gate-telemetry` — per (tenant, channel): total/warn/held/failed/retry/
  publish/export + top gate fail per channel (kèm severity block-vs-warn) + filter date. GROUP BY server-side (unnest
  gate_ledger/flags trong SQL). Publish từ `publish_log`, export từ `audit_log`.
- FE `platform-stats/page.tsx`: thêm `GateTelemetrySection` (bảng sortable + filter pill kênh + date range + bar list
  top-gate màu theo severity). KHÔNG tạo trang mới. Test mới `test_aa615_gate_telemetry.py` (6 test).

**Steering:** thêm `.kiro/steering/language-convention.md` (tiếng Việt trao đổi / tiếng Anh code; ngoại lệ log+memory
tiếng Việt). Bổ sung 2 quy tắc vào `session-workflow.md`: (1) định nghĩa "xong" issue backend = merge+CI+deploy COMPLETED+
verify endpoint live; (2) file tạm ghi trong `.tmp-session/` (không ghi ra `~/`, tránh Kiro hỏi allow).

## Thay đổi Hạ tầng (AA-CIS-Infra)
**AA-627** (Infra #63 + fix #65): Lambda `aa-cis-dev-dfs-balance-check` (in-VPC, zero-dep urllib POST) + EventBridge
Scheduler cron(15 6 * * ? *) daily. Fix #65: schedule dời từ group `default` → `aa-cis-dev-acp` (CI role IAM chỉ scope
`schedule/aa-cis-dev-acp/*` → schedule ở `default` làm CI Terraform Plan fail 403). terraform applied aa365 (8 added, sau đó 1/1 replaced).

## Bằng chứng verify
- **AA-627**: migration 159 applied Dev; endpoint live (balance $49.87); manual lambda invoke → 200; schedule ENABLED.
- **AA-613/614/615**: mỗi PR CI xanh đủ 5 job (Lint/Security/Unit/Integration/Docker) + Vercel SUCCESS.
- **AA-614 test**: pytest 96 pass (test_aa450_content_writing_service + test_aa450_v1_content_writing).
- **AA-615 test**: pytest 6 pass (test_aa615_gate_telemetry); `-k "a4 or platform"` 617 pass (không regression).
- **Deploy Dev**: ECS `aa-cis-dev-api` rollout **COMPLETED** taskDef **312** (sau merge #411/#412).
- **Endpoint live**: `GET /admin/a4/gate-telemetry` → **HTTP 200**, shape `{"data":{"by_tenant_channel":[],"top_gate_failures_by_channel":[]}}`
  (rỗng vì Dev DB reset audit — đúng mong đợi).

## Còn lại (đầu phiên sau)
- Epic AA-607 (redesign social tenant) — 3 sub AA-613/614/615 đã Done. Kiểm còn sub nào của AA-607/AA-612 chưa làm không.
- (Từ S189) Blocker AA-624 — Bedrock Batch acc3 chưa AWS-enable (AWS case 178979743800653) chặn AA-621/606/G4/G5. Chờ AWS.
- Chưa có dữ liệu thật để nhìn telemetry AA-615 (Dev DB reset) — khi có content_piece/publish/export thật thì verify lại UI.

## Lưu ý kỹ thuật (cho phiên sau)
- **Định nghĩa "xong" issue backend**: merge + CI + deploy Dev rollout COMPLETED + curl endpoint live 200 (đã lưu steering).
- **File tạm**: ghi `.tmp-session/` trong workspace, KHÔNG `~/` (Kiro hỏi allow file ngoài workspace kể cả autopilot). Đã lưu steering + .gitignore.
- pytest full: `env -u AWS_PROFILE AWS_ACCESS_KEY_ID=dummy ... .venv/bin/python -m pytest tests/unit/ -q` (AWS_PROFILE rỗng làm vỡ test_dlq_classifier → phải UNSET).
- flake8 tại `~/.local/bin/flake8` `--max-line-length=120 --extend-ignore=E203,W503,E302,E303,E305,E221,F401,E128,E124,E401,E127,E126,B008,E701,F821,F841,W291,E272,W293,E231`.
- **ĐỢI CI xanh MỚI merge** (bài học: từng merge sớm → CI Terraform Plan fail vào main). gh pr merge `--squash`. Branch từ `origin/main`.
- gh PR body dùng `--body-file` (KHÔNG inline). gh network hay treo → bg process + timeout + đọc file.
- WSL lag khi tích tụ nhiều bg process → STOP process sau khi dùng.
