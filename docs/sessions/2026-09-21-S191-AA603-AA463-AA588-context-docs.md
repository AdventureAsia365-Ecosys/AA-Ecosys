# S191 — 2026-09-21 (Kiro, AA-Ecosys)

Dọn cluster N7/N8 chết (AA-603), fix đếm published_tours + điều tra brand_audit/seo_context
(AA-463), gỡ trust OIDC org cũ (AA-588), và viết bộ CONTEXT.md cho 3 repo + cập nhật
ecosystem-architecture.md.

## Trạng thái

- **AA-603 (DROP N7/N8 dead cluster):** Done + verify live.
- **AA-463 (fix đếm published_tours + điều tra item 1/3):** Done + verify live.
- **AA-588 (gỡ trust OIDC org cũ):** Done + verify live.
- **AA-628:** tạo mới (Low) — feedback loop engagement cho T-series (future).
- **AA-624 (Bedrock Batch acc3):** vẫn blocked phía AWS (case 178979743800653), không đụng.
- **Bộ CONTEXT.md + ecosystem-architecture.md:** đã viết xong local, push qua PR (mỗi repo 1 PR).

## Thay đổi Codebase

### AA-603 — gỡ N7/N8 write-path chết (repo AA-CIS-App, PR #413, merge 6bb1465)
- Xoá 13 file source: `trust_ramp` / `packets` / `research` / `faq` / `adapt` / `pipeline` /
  `repair` / `slot_runner` / `generation` / `content_metrics` / `lock_status` + router+service
  `acp_health`.
- Xoá FE admin `app/admin/run-health/page.tsx` + 21 file test liên quan.
- Sửa `main.py`, `admin_a4.py`, `v1_planning.py`, `allocator.py`, `constants.py`,
  `AdminSidebar.tsx`, `middleware.ts`, `test_aa448_v1_planning.py`.
- 1772 test pass, flake8 clean, `next build` OK.
- DROP 5 bảng chết (script `docs/aa603-drop-n7n8-tables.sql`, dry-run→commit):
  `content_metric_snapshot`, `content_pieces`, `content_packets`, `acp_v2_slots`, `acp_v2_runs`.
  **GIỮ** `tenant_tour_pages` (còn dùng).

### AA-463 — fix đếm published_tours (repo AA-CIS-App, PR #414, merge 07d2104)
- Sửa 8 query `COUNT(*)` trong `admin.py` + `admin_pipeline.py`: thêm `master_status <> 'trashed'`
  (trước đó đếm cả tour đã trash → sai số published).
- Điều tra item 1/3 (chỉ báo cáo, không sửa): 52 tour `manual_check` đều score ≥ 8 (do brand_audit
  chấm strict); 139/224 = 62% `seo_context` null.

### CONTEXT.md + ecosystem-architecture.md (repo tương ứng)
- `apps/AA-CIS-App/CONTEXT.md`: **augment** — thêm section "Repo Context" ở đầu (stack/deploy/ranh
  giới), giữ nguyên phần "Content Pipeline Domain" (glossary AA-539/540).
- `apps/AA-TripPlanner-Web/CONTEXT.md`: **mới** — B2C map planner, Next.js FE + 2 Lambda
  (browse stateless/cacheable, assembly stateful+Bedrock), trip event-sourced, taste-vector+geo,
  schema `tripplanner.*` sở hữu + `shared.destinations` read-only.
- `infra/AA-CIS-Infra/CONTEXT.md`: **mới** — 3 account, `accounts/aa365` là stack chính + modules,
  state S3 + DynamoDB lock, OIDC/CI-CD, cách apply, ranh giới "App owns code, Infra owns resources".
- `docs/ecosystem-architecture.md` (repo gốc): sửa §5 (đính chính fact OIDC — org CÓ bật
  include-repo-id) + §7 (thêm 3 CONTEXT.md vào nguồn tham chiếu).

## Thay đổi Hạ tầng

### AA-588 — gỡ trust OIDC org cũ (repo AA-CIS-Infra, PR #66, merge 4c71640)
- Sửa `accounts/aa365/cicd.tf` + `tripplanner.tf`: bỏ trust org cũ `AdventureAsia365-CIS`.
- **Đính chính fact OIDC (qua CloudTrail):** org `AdventureAsia365-Ecosys` **CÓ** bật "include repo
  ID in subject" (precheck doc g5 ghi "org KHÔNG bật" là SAI). `sub` thật dạng
  `repo:AdventureAsia365-Ecosys@<orgid>/<repo>@<repoid>:<ref>`.
- Trust cuối:
  - `cicd.tf` → `repo:AdventureAsia365-Ecosys*/*:*`
  - `tripplanner.tf` → `repo:AdventureAsia365-Ecosys*/AA-TripPlanner-Web*:*`
- Pattern phẳng `repo:AdventureAsia365-Ecosys/*:*` (không `*`) **fail AccessDenied** — tái hiện qua
  CI Terraform Plan #94, sau khi fix (commit d2a5a0f) → apply → CI xanh lại → merge.

## Bằng chứng verify

- **AA-603:** PR #413 merged (6bb1465); deploy Dev taskDef **313 COMPLETED**; endpoint gỡ → 404,
  endpoint giữ → 401/403 (live). DROP: verify chỉ còn `tenant_tour_pages`.
- **AA-463:** PR #414 merged (07d2104); deploy Dev taskDef **314 COMPLETED**. Verify DB:
  published_tours 116 all active / 0 trashed (bug latent trước fix).
- **AA-588:** PR #66 merged (4c71640); verify live `aws iam get-role` (trust chỉ còn org mới);
  CI Terraform Plan re-run SUCCESS.

## Còn lại

- Bộ CONTEXT.md + ecosystem-architecture: đang push qua PR (4 PR: App/TripPlanner/Infra + repo
  gốc). Chờ Nghiệp merge tay theo quy trình.
- AA-624: blocked phía AWS, chờ phản hồi case.
- AA-628: Low, future (feedback loop engagement).

## Lưu ý kỹ thuật cho phiên sau

- **OIDC fact (QUAN TRỌNG):** org bật include-repo-id → trust PHẢI dùng `repo:AdventureAsia365-Ecosys*/*:*`
  (wildcard sau tên org). Đừng quay lại pattern phẳng.
- apply prod KHÔNG cần: `terraform-apply.yml` là `workflow_dispatch` only, cùng root `accounts/aa365`
  + state `dev/terraform.tfstate` → dispatch ra "No changes" nếu đã sync.
- WSL shell nuốt ký tự + `cd` path Windows lỗi → dùng absolute path + `-C`/`--prefix`, tránh `cd`.
  `read_file` bị nối đôi path khi truyền absolute → dùng path tương đối từ workspace root.
- Định nghĩa "xong" issue backend: merge + CI + deploy COMPLETED + verify endpoint live (4 điều kiện).
