# S185 — AA-608 follow-up verify + 3 sub AA-607 (611/609/612a) (Kiro, 2026-09-17)

Tác nhân: **Kiro**. Tiếp nối S184.

## Trạng thái
Hoàn tất: (1) đóng triệt để 2 điểm sót AA-608 (MEAL_TIME_INVENTED false-positive + Korea SEO_META
kẹt) qua PR #390 + verify live; (2) 3 sub AA-607 mức phụ — AA-611 (gỡ s1-from-atom), AA-609
(gỡ star no-op), AA-612 phần (a) (fix bug T2 brand fetch). Tất cả 4 PR (#390/#391/#392/#393)
đã merged + CI xanh.

## Thay đổi Codebase (repo AA-CIS-App)

### AA-608 follow-up — PR #390 (`fix(s1): precise meal-time audit + flag_fix json salvage`), merge `82aec2f`
- `services/content_generation/brand_audit_node.py`: regex `ITIN_MEAL_INVENTED` thu hẹp — chỉ bắt
  meal-as-logistics (`Meals:`, `(B,L,D)`, `lunch included/provided/served`), KHÔNG bắt narrative
  prose. (Trước: `\b(breakfast|lunch|dinner)\b` bắt mọi mention → ~26/30 false-positive.)
- `services/content_generation/flag_fix_node.py`: thêm `repair_json` salvage — fix-pass output
  malformed JSON trước đây throw JSONDecodeError → `fix_pass_applied=false` → tour kẹt hitl. Giờ
  salvage nên fix pass áp được (nguồn cơn Korea SEO_META kẹt).
- `tests/test_brand_audit_node.py` + `tests/unit/test_brand_audit_meal_time.py`: align test theo
  policy mới (narrative meal + bare 24h clock KHÔNG flag; thêm case `(B,L,D)` + `lunch included`).
  Regression: regex mới làm 3 test cũ fail Unit Tests lần đầu → đã sửa (commit `0cfef14`).

### AA-611 — PR #391 (`chore: remove dead s1-from-atom`), merge `42148e8`
- Gỡ `api/main.py` import+include `v1_s1_from_atom_router`; xóa `api/routers/v1_s1_from_atom.py`
  + `services/content_generation/s1_from_atom.py`.
- `services/eval/regression.py`: gỡ nhánh s1_from_atom (eval branch + fixture tour ids +
  `_detect_regression` gate-pass-rate); chỉ còn s1_old.
- `.github/workflows/eval-regression.yml`: gỡ option `s1_from_atom`/`both`.
- Xóa test: `test_aa306_s1_from_atom.py`, `test_aa289_s1_from_atom_run_log.py`,
  `tests/verify_scripts/aa392_sonnet_writer_verify.py`.
- Sửa test còn ref module đã xóa (CI bắt, local collect miss): `test_aa289_eval_regression.py`
  (gỡ 4 test s1_from_atom), `test_aa298_judge.py` (2 test context-isolation — bỏ import
  `_GROUNDING_SYSTEM_PROMPT`, giữ check phrase "CLOSED WORLD RULE"), `test_aa318_prompt_log_versioning.py`
  (gỡ test_generate_s1_from_atom_logs_full_prompt_text).
- GIỮ: comment tham chiếu trong acp_produce (không import); migration 088 + bảng
  `acp_contract.s1_from_atom_runs` (DB concern, thuộc AA-594).

### AA-609 — PR #392 (`chore: remove dead atom-curation star`), merge `f6650e7`
- `services/acp_planning/allocator.py`: gỡ hệ số `(1.5 if a.starred)`; sửa docstring sai (claim
  compute_slot_grid là live acp_angle_gate entry point — thực tế 0 production caller).
- `services/acp_planning/models.py`: gỡ `AtomRecord.starred`.
- `tenant_pool.py`/`quarter.py`: gỡ `starred` khỏi SELECT + `_row_to_atom`.
- `api/routers/admin_atoms.py`: gỡ `starred` khỏi AtomPatchRequest, patch logic, list SELECT, RETURNING.
- `api/routers/v1_marketplace.py`: gỡ aggregate `starred_atom_count`.
- FE `frontend/app/admin/atom-curation/page.tsx`: gỡ nút Star + toggleStar/starSelected + bulk-select
  checkbox (chỉ tồn tại để bulk-star). FE `MarketplaceTab.tsx`: gỡ display "N starred".
- Test: repoint star-only case sang text/deleted; gỡ starred mock keys.
- GIỮ: column DB `acp_contract.tour_atoms.starred` + INSERT atomize (ghi default false). Drop column
  đã note vào **AA-603**. `deleted` giữ nguyên.

### AA-612(a) — PR #393 (`fix: tenant T2 brand fetch loads brand-diff fields`), merge `1f863e3`
- `api/routers/v1_tours.py`: SELECT brand tenant thêm `core_idea, customer_segment, customer_mindset,
  voice_examples, good_examples` + map vào `brand_rules` (đúng key `_rewrite_tour` đọc, giống admin
  `_execute_run_tour`). json-parse `voice_examples` (asyncpg no-jsonb-codec). → judge brand-fit giờ
  chạy cho tenant có brand thật (trước bị skip `no_brand_profile`).
- Test mới `tests/unit/test_aa612a_tenant_brand_judge_gate.py`.
- Admin judge-skip (data-driven, row 'default' cột rỗng) giữ nguyên.

## Thay đổi Hạ tầng
- KHÔNG có thay đổi hạ tầng (không terraform). Deploy Dev tự chạy qua GitHub Actions sau mỗi merge.
- Bastion `i-006e9b5bc05c6861f` (AA-602) VẪN CHẠY — chưa terminate (quyết để phiên sau, xem Còn lại).
- DB tunnel SSM (localhost:15432) đã mở trong phiên (background process) — nên đóng khi hết dùng.

## Bằng chứng verify
- **AA-608 rerun** (batch `h2-verify2-haiku-2026-09-17`, 27 tour đa nước ≥5 ngày, code mới live Dev):
  approved 27/27=100%, hitl 0, manual_check 0, MEAL_TIME_INVENTED ~9/27≈33% (đều fixed+approved,
  giảm từ pre-fix ~87%), SEO_META_TOO_LONG kẹt 0 (Korea fix chạy đúng). avg score ~8.8. Query trực
  tiếp `silver_aa_internal.generated_content` + `quality_scores` qua tunnel. Đã comment AA-608.
- **AA-611/609/612a**: py_compile clean; unit collection 1926→1973→1979 tests 0 errors; targeted tests
  pass; full tests/unit 1919 passed (2 fail test_aa324 do thiếu OPENAI_API_KEY env LOCAL — không
  liên quan, CI có key thật → CI 5/5 xanh cả 3 PR).
- 4 PR CI 5/5 required check (Lint/Security/Unit/Integration/Docker) SUCCESS + merged. Vercel preview
  KHÔNG phải required check (xác nhận qua branch protection API) → PR #391 Vercel FAILURE không chặn.

## Còn lại (việc đầu phiên sau S186)
1. **AA-608 batch verify data = rác trong DB Dev** — 105 distinct tour (161 gen_content, 97 gold,
   7,579 atoms) từ 3 batch `h2-verify-haiku`/`h2-verify2-haiku`/`h3-measure-haiku`. Đã ghi nợ vào
   **AA-600 (G5)** — cuốn khi rerun full 763 tour (có backup). KHÔNG xóa tay giữa phiên.
2. **AA-612 phần (b)** — điều tra T7-T11 (social content tenant) đối chiếu Ms.Thư. STEP0 trước, chờ
   Nghiệp duyệt. AA-612 GIỮ mở.
3. **AA-606 Bedrock Batch** — CHẶN G4 (rerun 763 tour realtime = 13-25h qua ECS 1 worker, không khả
   thi). Còn: mở prefix S3 s1-rewrite + code submit/poll/ingest + xác nhận quota. IAM acc1+acc3 đã
   verify (S185). Đây là nhánh chính mở đường rerun.
4. **AA-610** rank-sum score ranking (điều tra, Medium).
5. **AA-603** — bổ sung drop column `tour_atoms.starred` (sau khi gỡ INSERT tenant_pipeline + deploy).
6. **AA-602** — quyết terminate bastion `i-006e9b5bc05c6861f` hay giữ (đang tốn phí + mở SG).
7. Đóng DB tunnel SSM localhost:15432 nếu còn mở.

## Lưu ý kỹ thuật (phiên sau)
- **PR xóa code: PHẢI chạy full `tests/unit/` (không chỉ `--collect`)** trước push — test call-time
  fail không hiện lúc collect (bài học AA-611: CI bắt 5 test còn ref module đã xóa).
- `.venv/bin/python` là interpreter đúng (plain `python` sai, thiếu deps — treo pytest).
- Chạy pytest local cần env: `JWT_SECRET=x AWS_ACCESS_KEY_ID=dummy AWS_SECRET_ACCESS_KEY=dummy
  AWS_DEFAULT_REGION=us-west-1 AWS_EC2_METADATA_DISABLED=true` (tránh MFA prompt lúc collect + JWT gate).
- Branch protection `strict: true` (branch phải up-to-date) + auto-merge 1-lúc-1 → khi merge nhiều PR
  song song phải sync `origin/main` vào từng branch còn lại (`git merge origin/main --no-edit` + push)
  mỗi khi PR trước merged. Required check = 5 CI job; Vercel KHÔNG required.
- AWS creds hết hạn giữa phiên → cần MFA re-auth:
  `export $(aws configure export-credentials --profile aa365-admin --format env | xargs) && unset AWS_PROFILE`
  (Nghiệp nhập MFA ở shell tương tác; shell của Kiro re-export non-interactive được khi session MFA còn sống).
- Tunnel: `aws ssm start-session ... i-006e9b5bc05c6861f ... localPortNumber 15432`. psql/psycopg2
  `host=localhost port=15432 user=aa_cis_admin dbname=aa_cis_dev`.
- Shell execute_bash chập chờn nặng (hay trả -1, không tạo file) → dùng control_bash_process, ghi
  output ra file rồi read_file.
- delete_file tool fail trên path WSL UNC → dùng `git rm` qua bash.
